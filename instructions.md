# Instructions

First we need to install the rendercv dependency :
    
    pip3 install "rendercv[full]"


Then we update the yaml and generate and compile the program:
        
    rendercv render CVAndre.yaml